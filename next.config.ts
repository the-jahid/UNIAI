import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['aceternity.com', 'i.ibb.co', 'i.ibb.co.com', 'images.unsplash.com', 'assets.aceternity.com'],
  },
};

export default nextConfig;
