import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['aceternity.com', 'i.ibb.co', 'images.unsplash.com', 'assets.aceternity.com', 's1.ticketm.net','i.ibb.co.com'],
  },
};

export default nextConfig;
